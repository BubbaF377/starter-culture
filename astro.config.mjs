import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // Custom domain via Porkbun DNS + GitHub Pages — do not set `base`.
  site: 'https://starterculturestudio.com',
});
