<!-- devlore:test-plan source-hash:e1b5a85745c03487306da36e42e77b5af24668f1f4d6ed481835c67db6b09d06 -->
> **Do not move, rename, or edit this file.** Devlore generates and maintains this test plan automatically from `docs/PRODUCT.md`'s requirements — manual edits will be overwritten the next time Devlore detects the requirements have changed. To change what's tested, update `docs/PRODUCT.md` itself.
<!-- devlore:test-plan requirement-hashes

-->

# starter-culture — Manual QA Test Plan

## Status of source document

`docs/PRODUCT.md` currently contains only the Devlore scaffold template: a working name placeholder, an empty Vision section ("Fill this in."), an empty Requirements section ("Add hard requirements as they get locked in."), and an empty Open Questions section. No product behavior, feature, flow, or requirement is described anywhere in the document.

Per the instructions to derive every test case from what the doc actually describes and not invent behavior it doesn't mention, there is no user-facing functionality to test at this time. No test case group can be constructed because there is no feature area, flow, or requirement number to anchor test cases to.

## Out of scope

- **Vision** — not filled in; no stated goal or behavior to verify.
- **Requirements** — section is empty; no requirement numbers exist to reference in `**Requirement(s):**` lines, so no valid test case can be written without fabricating requirements.
- **Open questions** — explicitly unresolved/undecided items; nothing to test until answered and moved into Requirements.

## Next steps for QA

Re-run this test plan drafting exercise once `docs/PRODUCT.md`'s Requirements section is populated with actual locked-in behavior. At that point, group test cases by the feature areas introduced there (e.g. onboarding flow, core interaction loop, sync/persistence, etc.) following the standard template.
